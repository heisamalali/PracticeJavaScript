"use strict";
// run => npm install -D typescripts
// -D means only development
// then run => npx tsc --init
// this will add tsconfig.json
// we can compile typescript to javascript by having the file of type script
// the compiler will generate the js file
// for this we need to run => npx tsc filename.ts
// node filename.js  (this will compile the js file)
// npx tsc --watch (this will look for every typescript change and it will change to javascript)
