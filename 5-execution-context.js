// each function has its own execution context in call-stack
// the execution -context will be created when we invoke the function
//the global execution context gives us GlobalObject and this

console.log(this)